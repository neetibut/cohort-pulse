# Backend base URL.
# Local dev: leave blank — defaults to http://localhost:3001.
# Production: your Render service URL, e.g. https://cohort-pulse.onrender.com
# (no trailing slash). Vite reads this at BUILD time — redeploy after changing it.
VITE_API_URL=
